language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-admin' ]; then cd .. && eval "mv $currentfolder generator-admin" && cd generator-admin; fi

